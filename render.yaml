services:
  - type: web
    name: wavecrm-api
    runtime: node
    region: virginia
    plan: starter
    buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm --filter @wave/database generate && pnpm --filter @wave/api build
    preDeployCommand: corepack enable && pnpm --filter @wave/database migrate:deploy
    startCommand: pnpm --filter @wave/api start:prod
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_URL
        sync: false
      - key: CORS_ORIGIN
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: APP_URL
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: EMAIL_FROM
        sync: false

  - type: web
    name: wavecrm-web
    runtime: node
    region: virginia
    plan: starter
    buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm --filter @wave/web build
    startCommand: pnpm --filter @wave/web start
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: NEXT_PUBLIC_API_URL
        sync: false
